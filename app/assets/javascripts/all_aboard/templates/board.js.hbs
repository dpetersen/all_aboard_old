{{partial "navbar"}}

{{outlet}}

{{#if slides.length}}
  <ol id="slides">
    {{each slides itemViewClass="App.PresentSlideView"}}
  </ol>
{{else}}
  <h3>You need to add some slides if you want to see something here.</h3>
{{/if}}
